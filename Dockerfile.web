FROM node:20-slim AS build

WORKDIR /app

COPY web/package.json web/package-lock.json* web/bun.lockb* ./
RUN npm install

COPY web/ .

# Recebe as variáveis como Build Args (passados pelo EasyPanel)
ARG SUPABASE_URL
ARG SUPABASE_KEY

# Vite precisa de VITE_ prefix — mapeia os args para o formato correto
ENV VITE_SUPABASE_URL=$SUPABASE_URL
ENV VITE_SUPABASE_KEY=$SUPABASE_KEY

RUN npm run build

FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
